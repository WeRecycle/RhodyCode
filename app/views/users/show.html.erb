<div id="sideContainer">
 <div class="row content">
   <center>
     <h4>Wecome</h4>
      <h2><%= @user.email %></h2>
      <%= image_tag @user.avatar.url, class: "profile" %>
      <h4>This is where I would say cool things that I know. I could put different langugages I know or things.</h6><br>
      Also could put the tags that they are interested in</h4><br>

      <%= @user.tags.each do %>
        <button type="button" class="btn btn-secondary"><%= tag.name %></button>
      <% end %>
    </center>
  </div>
</div>

<div id="sideContainerLeft">
  <center><h1>Active Projects</h1></center>
  <p>
    <div class="dashboard-container">
      <table class="table">
        <thead>
          <tr>
            <th>Project Name</th>
            <th>Description</th>
          </tr>
        </thead>
        <tbody>
          <% @projects.each do |project| %>
            <tr>
              <td>
                <h3><%= link_to project.title, project_path(project) %></h3>
                <%= image_tag project.image.url %>
              </td>

              <td>
                <%= project.description %>
                <br><br>
                <p align="right">Last Updated : <i> October 24, 2016</i></p>

                <hr>
                <% project.tags.each do |tag| %>
                  <a href="<%= tag_path(tag) %>" class="btn btn-primary"><%= tag.name %></a>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

<%= simple_form_for @project do |f| %>
  <%= f.input :title, label: "Project Title" %>
  <%= f.input :description %>
  <%= f.input :image, as: :file %>
  <%= f.association :tags %>
  <%= f.button :submit %>
<% end %>
